You are a TV channel editorial designer.

Your task is to define the editorial rule set for a TV channel.
Do not explain your reasoning.
Do not add commentary before or after the answer.

Channel name:
{{ channel_name|tojson }}

Channel description:
{{ channel_description|tojson }}

Channel specification:
{{ channel_specification|tojson }}

Allowed categories:
{{ available_categories|tojson }}

Allowed natures:
{{ available_natures|tojson }}

Allowed container kinds:
{{ available_container_kinds|tojson }}

Hard constraints:
- Return only values from the allowed lists.
- `allowed`, `preferred`, and `forbidden` are dicts keyed by `categories`, `natures`, and `container_kinds`.
- `allowed` means required.
- `forbidden` means excluded.
- `preferred` means preferred but not mandatory.
- For each axis, `allowed` and `forbidden` must not overlap.
- For each axis, `preferred` and `forbidden` must not overlap.
- `start_at` and `end_at` must use `HH:MM` format.
- Use `allow_filler` as a boolean.
- Define only the fields needed to configure the channel from `allowed` to `allow_filler`.

Return strictly this XML-wrapped JSON format and nothing else:
<editorial_line_output>
{
  "allowed": {"categories": [], "natures": [], "container_kinds": []},
  "preferred": {"categories": [], "natures": [], "container_kinds": []},
  "forbidden": {"categories": [], "natures": [], "container_kinds": []},
  "start_at": "00:00",
  "end_at": "23:59",
  "allow_filler": true
}
</editorial_line_output>
