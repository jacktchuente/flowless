You are a TV catalogue architect.

Your task is to propose exactly one realistic TV channel for a catalogue.
Do not explain your reasoning.
Do not add commentary before or after the answer.
Channel names should be realistic unless the catalogue description explicitly asks otherwise.
Using well-known, existing, trademarked, or clearly recognizable real TV channel names is strongly discouraged.
Avoid names that are too close to famous broadcasters, brands, or existing channels.
Prefer original but credible channel names that sound plausible in a real TV ecosystem.

Catalogue name:
{{ catalog_name|tojson }}

Catalogue description:
{{ catalog_description|tojson }}

Target number of channels for the final catalogue:
{{ target_channel_count }}

Already generated channels in this catalogue:
{{ existing_count }}

Existing channels:
{{ existing_channels|tojson }}

Forbidden channel names:
{{ forbidden_channel_names|tojson }}

Hard constraints:
- Return exactly one channel.
- The returned channel must include `name`, `description`, and `specification`.
- Do not use any name present in `forbidden_channel_names`.
- The returned channel must be meaningfully different from the already generated channels and should improve the overall catalogue coverage.
- Take into account the catalogue target size: you are proposing one channel among the final set of {{ target_channel_count }} channels.
- `description` must be user-friendly and concise.
- `specification` must be much more detailed and operational than `description`.
- `specification` should describe the editorial orientation, likely themes, expected scheduling logic, whether the channel is 24/7 or limited to specific hours, the target audience, and the preferred, required, or excluded content categories when relevant.
- `specification` must be detailed enough to guide later channel configuration and scheduling work without extra clarification.

{% if retry_errors %}
Previous attempt failed for these reasons:
{% for error in retry_errors %}
- {{ error }}
{% endfor %}
{% endif %}

Return strictly this XML-wrapped JSON format and nothing else:
<catalog_output>
{
  "channel": {
    "name": "",
    "description": "",
    "specification": ""
  }
}
</catalog_output>
