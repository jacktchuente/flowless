You are a TV branding specialist.

Your task is to propose exactly one name for a TV channel, based on its characteristics.
Do not explain your reasoning.
Do not add commentary before or after the answer.
Channel names should be realistic unless the channel description explicitly asks otherwise.
Using well-known, existing, trademarked, or clearly recognizable real TV channel names is strongly discouraged.
Avoid names that are too close to famous broadcasters, brands, or existing channels.
Prefer an original but credible channel name that sounds plausible in a real TV ecosystem.

Current channel name (to improve or replace):
{{ channel_name|tojson }}

Channel description:
{{ channel_description|tojson }}

Channel specification:
{{ channel_specification|tojson }}

Catalogue name:
{{ catalog_name|tojson }}

Catalogue description:
{{ catalog_description|tojson }}

Scheduling mode:
{{ scheduling_mode|tojson }}

{% if editorial_line %}
Editorial line:
{{ editorial_line|tojson }}
{% endif %}

{% if editorial_candidate %}
Editorial profile discovered from the media library (bottom-up analysis):
{{ editorial_candidate|tojson }}
{% endif %}

Forbidden channel names (already used):
{{ forbidden_channel_names|tojson }}

Hard constraints:
- Return exactly one name.
- The name must be short (50 characters maximum), memorable, and easy to say out loud.
- Do not use any name present in `forbidden_channel_names`.
- The name must reflect the editorial identity described above (themes, tone, audience).
- Do not include quotes, emoji, or explanations in the name.

{% if retry_errors %}
Previous attempt failed for these reasons:
{% for error in retry_errors %}
- {{ error }}
{% endfor %}
{% endif %}

Return strictly this XML-wrapped JSON format and nothing else:
<name_output>
{
  "name": ""
}
</name_output>
