import {Component, Inject} from '@angular/core'; import {FormGroup, ReactiveFormsModule} from '@angular/forms'; import {NgIf} from '@angular/common'; import {MAT_DIALOG_DATA, MatDialogModule, MatDialogRef} from '@angular/material/dialog'; import {MatButtonModule} from '@angular/material/button'; import {MatProgressSpinnerModule} from '@angular/material/progress-spinner'; import {FormlyFieldConfig, FormlyModule} from '@ngx-formly/core'; import {FormOptions, FormSuggestionResponse, GridPayload} from '@project-interfaces/tv-channel'; import {TvChannelService} from '@project-services/tv-channel.service'; import {DialogContainer1Component} from '@project-templates/dialog-container1/dialog-container1.component'; import {SuggestionPanelComponent} from '@project-templates/suggestion-panel/suggestion-panel.component';
@Component({selector:'app-grid-edit-dialog',standalone:true,imports:[ReactiveFormsModule,NgIf,MatDialogModule,MatButtonModule,MatProgressSpinnerModule,FormlyModule,DialogContainer1Component,SuggestionPanelComponent],templateUrl:'./grid-edit-dialog.component.html'})
export class GridEditDialogComponent { form=new FormGroup({}); model:GridPayload; fields:FormlyFieldConfig[]; isSubmitting=false;isSuggesting=false;error:string|null=null; constructor(private service:TvChannelService,private ref:MatDialogRef<GridEditDialogComponent>,@Inject(MAT_DIALOG_DATA) public data:{channelId:string|number,postFillerPolicy:string|number|null,formOptions:FormOptions}){this.model={post_filler_policy:data.postFillerPolicy};this.fields=[{key:'post_filler_policy',type:'select',props:{label:'Politique de remplissage',options:[{label:'Aucune',value:null},...data.formOptions.filler_policies.map(x=>({label:`${x.name} (${x.duration_seconds}s)`,value:x.id}))]}}]}
suggest(context:string){this.isSuggesting=true;this.error=null;this.service.suggestForm(this.data.channelId,{form_kind:'grid',user_context:context,current_values:{...this.model}}).subscribe(r=>{this.isSuggesting=false;if(!r.isOk){this.error=this.detail(r.body);return}const v=(r.body as FormSuggestionResponse).values;Object.assign(this.model,v);this.form.patchValue(v)})} save(){if(this.form.invalid||this.isSubmitting)return;this.isSubmitting=true;this.service.updateGrid(this.data.channelId,this.model).subscribe(r=>{this.isSubmitting=false;if(!r.isOk){this.error=this.detail(r.body);return}this.ref.close({saved:true})})} private detail(v:any){return v?.error?.detail??(v?.error?JSON.stringify(v.error):'Enregistrement impossible.')}}
